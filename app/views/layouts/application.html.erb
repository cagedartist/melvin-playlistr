<!DOCTYPE html>
<html>
<head>
  <title>Sharemusic</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
  <header>
    <h1>Playlistr <span>&lt;-cool name and logo, here</span></h1>
    <nav>
      <% if current_user %>
      Hi, <%= current_user.login %> <%= '(ADMIN) ' if current_user.admin? %>--
      <%= link_to "Playlist", playlist_user_path(current_user) %> | 
      <%= link_to 'People', users_path %> |
      <%= link_to "Music", current_user %> | 
      <%= link_to 'Account', edit_user_path(current_user) %> |  
      <%= link_to "Log out", logout_path %>
      <% end %>
    </nav>
  </header>
  <div id="main_body">
<%= yield %>
  </div>
  <footer>
    Demo app by Bob Melvin
  </footer> 
</body>
</html>

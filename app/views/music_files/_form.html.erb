<%# locals: user (required); music_file (required) %>
<%= form_for([user, music_file]) do |f| %>
  <% if music_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(music_file.errors.count, "error") %> prohibited this music_file from being saved:</h2>

      <ul>
      <% music_file.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :artist_name %><br />
    <%= f.text_field :artist_name %>
  </div>
  <div class="field">
    <%= f.label :duration %><br />
    <%= f.text_field :duration %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
